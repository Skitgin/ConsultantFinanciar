name: Keep Azure App Awake

on:
  schedule:
  
    - cron: '*/15 * * * *'
  workflow_dispatch: 
jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Health Endpoint
        run: |
          curl -s -o /dev/null -w "%{http_code}" consultanti-h6chdwgdh4e8fxdz.canadacentral-01.azurewebsites.net/health
